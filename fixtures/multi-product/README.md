# FIXTURE — a two-product repo (PLAN 4.14)

Two document sets under apps/, plus a workspace marker. /health must
report this as INFO (unsupported shape, not broken) and every
document-reading skill must name both candidate sets and STOP rather
than silently picking one.
