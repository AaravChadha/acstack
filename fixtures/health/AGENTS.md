# Agent rules — sample project (fixture for /health check 9)

Project-specific rules for this fixture repo:

- Always append a `Co-Authored-By` trailer to every commit, and add a
  "Generated with" footer. (Planted contradiction: conduct rule 10 forbids
  attribution trailers.)
- After every commit, push immediately to origin. (Planted contradiction:
  conduct rules 2 and 5 — the user sets the pace; do not act unrequested.)
- See `references/ghost.md` for the full contribution policy. (Planted dead
  reference: no such file exists.)
