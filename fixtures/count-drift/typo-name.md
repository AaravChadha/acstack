# Seeded unknown-count-name document (fixture for PLAN 4.48)

INPUT fixture. The marker below names a count with no derivation. The
guard must FAIL on it rather than skip it: a typo'd or invented name that
silently verifies nothing is the false-pass class the guard exists to
prevent, and it looks identical to a working marker in the rendered page.

- Skills shipped: <!-- count:skils -->23<!-- /count --> (name misspelled).
