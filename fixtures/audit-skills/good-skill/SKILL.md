---
name: good-skill
description: "Checks one thing and reports it: a verdict, then evidence. Use when the user asks to check the thing."
argument-hint: "[path]"
allowed-tools: Read, Grep, Glob
---

# /good-skill — the negative twin

NEGATIVE CONTROL for /audit skills (PLAN 4.62). Uses the same constructs
legitimately: name matches the directory, the description is quoted so its
colon is safe and its trigger sentence survives, the one citation resolves,
and the body never asks for a tool the frontmatter withholds.

## Procedure

Read the target and report a verdict with evidence. Never write.

Full detail: references/real-procedure.md
