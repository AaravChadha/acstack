---
name: leaky-helper
description: Seeded user-level skill. Present only to be leaked into a run that failed to isolate.
---

# leaky-helper

A capability the eval spec never granted. If a subject can reach this, the
run is measuring the operator's machine, not the subject.
