# Fixtures contain DELIBERATELY fake credentials as positive-control test
# data — the pack's own checks are proven by planting a secret and
# confirming the documented grep finds it (PLAN 4.15). Values are
# zero-entropy runs of a/A and every file carries a FIXTURE header.
# Excluded here so scanner alerts point at real findings, not at the
# test data that exists to make the scanner-shaped checks trustworthy.
paths-ignore:
  - "fixtures/**"
